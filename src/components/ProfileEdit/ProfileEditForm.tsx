import * as React from 'react';


interface IProps {
}

export const ProfileEditForm: React.FC<IProps> = (props) => {
    const {children} = props;
    return (
        <div>

        </div>
    );
};