import { makeStyles } from '@mui/styles';


export const useStyles = makeStyles({
    timeRoot:{
       fontSize:'2rem',
        fontFamily:"monospace",
        color:'gray',
        whiteSpace:'nowrap'
    },
});