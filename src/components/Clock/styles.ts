import { makeStyles } from '@mui/styles';


export const useStyles = makeStyles({
    timeRoot:{
       fontSize:'1rem',
        fontFamily:"monospace",
        color:'gray',
        whiteSpace:'nowrap'
    },
});